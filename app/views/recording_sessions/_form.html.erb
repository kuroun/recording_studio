<%= form_for(@recording_session) do |f| %>
  <% if @recording_session.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@recording_session.errors.count, "error") %> prohibited this recording_session from being saved:</h2>

      <ul>
      <% @recording_session.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>
  <h3>Room: <%=Room.find(session[:room_id]).room_name %></h3>
  <div class="field">
    <%= f.hidden_field :room_id, :value => session[:room_id] %>
  </div>
  <div class="field">
    <%= f.label :start_time %><br>
    <%= f.time_select :start_time %>
  </div>
  <div class="field">
    <%= f.label :end_time %><br>
    <%= f.time_select :end_time %>
  </div>
  <div class="field">
    <%= f.label :description %><br>
    <%= f.text_field :description %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
